# Example Environment Variables
# Copy this file to .env.development and .env.production
# and fill in your actual API keys

# =============================================================================
# CLIENT-SIDE VARIABLES (prefixed with VITE_)
# These are embedded in the browser bundle and publicly visible
# =============================================================================

# Gemini API Key (for voice transcription widget)
# This MUST be client-side because it uses WebRTC for live audio
# IMPORTANT: Restrict this key in Google Cloud Console:
#   1. Go to https://console.cloud.google.com/apis/credentials
#   2. Edit your API key
#   3. Set HTTP referrer restrictions to your domain
#   4. Restrict to only "Generative Language API"
VITE_GEMINI_API_KEY=your_gemini_api_key_here

# =============================================================================
# SERVER-SIDE VARIABLES (NO VITE_ prefix)
# These stay on the server and are NEVER exposed to the browser
# Set these ONLY in your hosting platform (Vercel Environment Variables)
# =============================================================================

# GitHub Personal Access Token (for GitHub widget)
# This is used by /api/github serverless function
# Get one at: https://github.com/settings/tokens (no scopes needed)
# DO NOT add this to .env files - set it in Vercel only!
# GITHUB_TOKEN=your_github_token_here
